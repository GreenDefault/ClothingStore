@model IEnumerable<DoAnWeb.Areas.Admin.Models.Size>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenSize)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenSize)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaSize }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaSize }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaSize })
        </td>
    </tr>
}

</table>
